// The landing page.
//
// Placeholder for now — one line saying what this is. It exists so that "/" is a
// front door rather than the jobs table, which meant the brand in the header had
// nowhere to link to and the dashboard opened straight into an operator view.
export default function HomePage() {
  return (
    <div className="py-16">
      <h1 className="text-3xl font-semibold tracking-tight">QueueFlow</h1>
    </div>
  )
}
